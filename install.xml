<?xml version="1.0" encoding="utf-8"?>
<modification>
	<name>OneCatalog Import</name>
	<code>onecatalog</code>
	<version>0.7.2</version>
	<author>OneCatalog</author>
	<link>https://github.com/OneCatalog/onecatalog-opencart</link>

	<!--
		Пункт меню «OneCatalog» в левой колонке админки (над «Catalog»).
		⚠️ OCMOD в OpenCart 3 сопоставляет <search> ПОСТРОЧНО — паттерн обязан быть
		ОДНОЙ физической строкой (многострочный <search> с error="skip" молча не срабатывает).
		Анкор — уникальная строка `'id' => 'menu-catalog',`; вставляем блок на строку выше
		(offset="1" → перед открывающей `$data['menus'][] = array(` для Catalog).
		error="skip": если разметка core отличается (тема/версия) — установка не падает,
		страница доступна через «Расширения → Модули → OneCatalog Import».
	-->
	<file path="admin/controller/common/column_left.php">
		<operation error="skip">
			<search><![CDATA['id'       => 'menu-catalog',]]></search>
			<add position="before" offset="1"><![CDATA[$data['menus'][] = array(
			'id'       => 'menu-onecatalog',
			'icon'     => 'fa-download',
			'name'     => 'OneCatalog',
			'href'     => '',
			'children' => array(
				array('name' => 'Import', 'href' => $this->url->link('extension/module/onecatalog/importPage', 'user_token=' . $this->session->data['user_token'], true), 'children' => array()),
				array('name' => 'Prices & stock', 'href' => $this->url->link('extension/module/onecatalog/b2bPage', 'user_token=' . $this->session->data['user_token'], true), 'children' => array()),
				array('name' => 'Import log', 'href' => $this->url->link('extension/module/onecatalog/logPage', 'user_token=' . $this->session->data['user_token'], true), 'children' => array()),
				array('name' => 'Settings', 'href' => $this->url->link('extension/module/onecatalog', 'user_token=' . $this->session->data['user_token'], true), 'children' => array())
			)
		);

		]]></add>
		</operation>
	</file>
</modification>
